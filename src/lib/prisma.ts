// Prisma has been removed from this project.
// This file remains as a harmless shim so any lingering imports won't
// crash the build while we fully remove Prisma from the repo.

const shim: any = undefined;

export const prisma = shim;

// NOTE: If you fully remove all references to prisma in your codebase,
// you can delete this file.